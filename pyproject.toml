[project]
name = "llama-web-manager"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi==0.140.7",
    "httpx>=0.28.1",
    "python-multipart>=0.0.32",
    "pyyaml>=6.0.3",
    "uvicorn==0.51.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
    "httpx2>=2.4.0",
]
